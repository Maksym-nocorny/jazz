# RETRO — <Client Name>

> Private learning journal — **not shown to the client**. After each major task, the owning agent
> appends a dated entry. **Every agent reads this file at the start of every session** so past
> mistakes are pre-applied, not re-discovered. (Keep a repo-level `RETRO.md` too for lessons that
> generalize across all projects.)

<!-- Entry template — keep it short and concrete:

## <YYYY-MM-DD> — <phase / task> — <agent>
- Took longer than it should have: <what + why>
- Missed (caught by user / judge / tester): <what + the fix>
- Will do differently next time: <one concrete rule>

-->
